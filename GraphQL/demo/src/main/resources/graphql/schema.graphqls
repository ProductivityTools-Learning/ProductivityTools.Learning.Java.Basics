type Query {
    bookById2(id: ID): Book
}
type Mutation{
    add(a: Int!, b: Int!): Int!
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}